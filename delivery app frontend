<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QuickEats Frontend</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <style>
        body { font-family: sans-serif; margin: 0; padding: 20px; background: #f4f4f4; }
        .container { max-width: 400px; margin: auto; background: white; padding: 20px; border-radius: 15px; box-shadow: 0 4px 10px rgba(0,0,0,0.1); }
        .restaurant-card { border: 1px solid #ddd; padding: 10px; margin-bottom: 10px; border-radius: 8px; cursor: pointer; }
        .restaurant-card:hover { border-color: #ff4500; }
        #map { height: 300px; width: 100%; border-radius: 10px; margin-top: 20px; display: none; }
        .status-bar { display: none; background: #ff4500; color: white; padding: 10px; border-radius: 8px; margin-top: 10px; text-align: center; }
    </style>
</head>
<body>

<div class="container">
    <h2>🍔 QuickEats</h2>
    <div id="restaurant-list">
        <div class="restaurant-card" onclick="placeOrder('Pizza Palace')">
            <strong>Pizza Palace</strong><br><small>15-25 min • $2.99 Delivery</small>
        </div>
        <div class="restaurant-card" onclick="placeOrder('Burger Town')">
            <strong>Burger Town</strong><br><small>10-20 min • $1.50 Delivery</small>
        </div>
    </div>

    <div id="status" class="status-bar">Driver is picking up your order...</div>
    <div id="map"></div>
</div>

<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
